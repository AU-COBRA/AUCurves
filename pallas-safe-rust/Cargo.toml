[package]
name = "pallas-safe-rust"
version = "0.1.0"
edition = "2021"
description = "Pallas (Pasta) base field — Fp — fiat-rust leaves + Bernstein-Yang CT inverse"
license = "Apache-2.0"

[lib]
name = "pallas"
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
fiat-crypto = { path = "../fiat-crypto/fiat-rust" }
safegcd     = { path = "../safegcd-rs" }

[dev-dependencies]
