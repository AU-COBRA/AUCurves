[package]
name = "vesta-safe-rust"
version = "0.1.0"
edition = "2021"
description = "Vesta (Pasta) base field — Fq — fiat-rust leaves + Bernstein-Yang CT inverse"
license = "Apache-2.0"

[lib]
name = "vesta"
path = "src/lib.rs"

[dependencies]
fiat-crypto = { path = "../fiat-crypto/fiat-rust" }
safegcd     = { path = "../safegcd-rs" }

[dev-dependencies]
